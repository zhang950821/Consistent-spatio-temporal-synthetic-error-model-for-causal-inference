# Split Rmd Chunks

These files were generated from `inst/original/vary-coefficient spatial temporal
synthetic error model_true.Rmd`.

Use `scripts/chunk_manifest.csv` to map each original chunk name to the generated
script. Long experiments should normally be launched through the stage scripts in
`scripts/`, not by sourcing individual chunk files manually.
